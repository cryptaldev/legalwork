import React from 'react'
import styles from './Footer.module.scss'
import { social } from '../config'

const Footer = () => (
    <footer className={styles.footer}>
        <small>
            Powered by Ocean Protocol
        </small>
    </footer>
)

export default Footer
